# Amiglot UI environment variables
# API base URL
NEXT_PUBLIC_API_URL=http://localhost:8080
# UI base URL (used for dev login links in test env)
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Allowed dev origins for HMR (comma-separated)
NEXT_ALLOWED_DEV_ORIGINS=http://localhost:3000
